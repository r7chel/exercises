#ifndef REF_H_
#define REF_H_

#include <stdbool.h>

int ref_largest(int array[], int length);
int ref_sum(int array[], int length);
void ref_sort(int array[], int length);
void ref_double_primes(int array[], int length);
void ref_negate_armstrongs(int array[], int length);

#endif
